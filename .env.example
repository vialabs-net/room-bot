# Anthropic API key (for message generation)
ANTHROPIC_API_KEY=


# GCP project ID
GCP_PROJECT_ID=recole-485714

# GCS bucket for WhatsApp auth state
WA_AUTH_BUCKET=recole-485714-room-bot-wa-auth

# Workspace ID (multi-tenant identifier)
WORKSPACE_ID=pgma

# Cloud Run service URL (for approval links in WhatsApp messages)
# Set after first deploy, or use localhost for local dev
SERVICE_URL=http://localhost:8080

# Port for Cloud Run
PORT=8080
